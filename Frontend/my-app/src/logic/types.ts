// types.ts

export interface Album {
    id: number;
    name: string;
    // Outros campos do álbum, se houver
  }
  export interface Photo {
    id: number;
    name: string;
    url: string;
    // Adicione outros campos conforme necessário
  }